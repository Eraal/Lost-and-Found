# Copy to /etc/ccs-lnf.env on the droplet and edit values
FLASK_ENV=production
SECRET_KEY=<generate-a-strong-secret>
DATABASE_URL=postgresql+psycopg2://<user>:<pass>@<host>:5432/<db>

# Allowed origins (comma separated)
CORS_ALLOW_ORIGINS=https://example.com

# Public base URL used by the backend when composing links for social posts
FRONTEND_PUBLIC_BASE_URL=https://example.com

# Optional first-admin bootstrap code. Remove after creating the first admin.
# ADMIN_INVITE_CODE=one-time-code

# Optional S3/Spaces if you configure object storage for uploads
# S3_BUCKET_NAME=
# S3_REGION=
# S3_ENDPOINT_URL=
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=
# S3_PUBLIC_URL_BASE=

# Token TTL
# AUTH_TOKEN_MAX_AGE=2592000
